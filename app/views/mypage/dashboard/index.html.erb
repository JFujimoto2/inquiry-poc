<h1 class="text-2xl font-bold mb-6">My Reservations</h1>

<% if @reservations.any? %>
  <div class="space-y-4">
    <% @reservations.each do |reservation| %>
      <div class="bg-white shadow rounded p-4">
        <div class="flex justify-between items-start">
          <div>
            <h3 class="font-medium text-lg"><%= reservation.facility.name %></h3>
            <p class="text-gray-600">
              Check-in: <%= reservation.check_in_date %>
              <% if reservation.check_out_date %>
                - Check-out: <%= reservation.check_out_date %>
              <% end %>
            </p>
            <p class="text-gray-600">Guests: <%= reservation.num_people %></p>
          </div>
          <div class="text-right">
            <span class="px-2 py-1 rounded text-sm <%= reservation_status_class(reservation.status) %>">
              <%= reservation.status.titleize %>
            </span>
            <div class="mt-2">
              <%= link_to "Details", mypage_reservation_path(reservation), class: "text-blue-600 hover:underline" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <p class="text-gray-500">You don't have any reservations yet.</p>
<% end %>
