<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "マイページ" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-indigo-700 text-white p-4">
      <div class="container mx-auto flex justify-between items-center">
        <div class="flex items-center space-x-6">
          <span class="font-bold text-lg">マイページ</span>
          <% if current_customer %>
            <%= link_to "ダッシュボード", mypage_root_path, class: "hover:text-indigo-200" %>
          <% end %>
        </div>
        <div>
          <% if current_customer %>
            <span class="mr-4"><%= current_customer.contact_name %></span>
            <%= button_to "ログアウト", mypage_session_path, method: :delete, class: "hover:text-indigo-200 bg-transparent border-0 cursor-pointer text-white" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-8 px-5">
      <% if notice.present? %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
          <%= notice %>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
          <%= alert %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
